# bakuon

## classes
* class Result -  Represents the 'success' or 'failure' of an operation, and holds an associated error message to describe the error when there's a failure.
* class Identifier -  Represents a string identifier, designed for accessing properties by name.
* class Workspace - Each plugin that wants to implement a workspace
* 