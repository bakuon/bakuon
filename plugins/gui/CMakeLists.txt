# ============================================================================
# plugins/gui —— 依赖 bakuon::gui 的运行时插件。
# 每新增一个插件，在这里加一行 add_subdirectory()。
# ============================================================================
add_subdirectory(example)
