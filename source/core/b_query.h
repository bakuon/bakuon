#pragma once

/// 查询/过滤 DSL
/**
  目前所有筛选都是 registry.each<A, B>(...) 硬编码的编译期
  模板参数组合。GUI 层的"大纲视图过滤""按标签搜索"这类运行期动
  态条件目前完全没有承载的地方。可以先做一个薄封装：运行期组合
  has<T>/hasAny<T> 谓词，内部仍然落到 each<>，不追求通用 ECS 查询语言。
 */
